package org.kramerlab.ml17.exercise;

import java.util.ArrayList;
import java.util.Set;
import java.util.HashSet;
import java.util.Random;
import org.kramerlab.ml17.teaching.HomeworkTodo;

public class Exercise_04_04 {
  /**
   * Expresses accuracy (TP + TN) / (TP + FP + FN + TN)
   * through prevalence, sensitivity, and specificity.
   *
   * @param prevalence P / (P + N)
   * @param sensitivity TP / P
   * @param specificity TN / N
   */
  public static double accuracy(
    double prevalence,
    double sensitivity,
    double specificity
  ) {
    throw new HomeworkTodo("implement accuracy");
  }
}